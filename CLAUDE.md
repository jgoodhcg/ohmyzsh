# Claude Code

See `AGENTS.md` for project policies and operating rules.

## Agent-Specific Instructions

- Never run tmux or killall commands during validation, even on isolated sockets — filesystem-only fixtures and `zsh -n` only.
- Use the `blueprint-update` skill to upgrade `AGENT_BLUEPRINT.md` and re-align.
